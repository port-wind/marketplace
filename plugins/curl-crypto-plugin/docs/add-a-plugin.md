# Add To Marketplace

When you mirror this plugin into the shared `port-wind/marketplace` repository:

1. Copy the plugin into `plugins/curl-crypto-plugin/`
2. Add a Cursor marketplace entry in `.cursor-plugin/marketplace.json`
3. Add a Claude Code marketplace entry in `.claude-plugin/marketplace.json`
4. Run `node scripts/validate-template.mjs` in the marketplace repo
