---
name: list-products
description: List ZenTao products through the local zentao CLI.
---

# List Products

1. Run `command -v zentao >/dev/null || npm install -g @leeguoo/zentao-mcp`.
2. Run `zentao products list`.
3. If successful, summarize the products in a short table or bullet list.
4. If the command reports missing login, tell the user to run `zentao login`.
