# Development Notes

The plugin is intentionally narrow:

- no automatic installation
- no Claude Code manifest
- no attempt to replace the upstream Cunzhi UI or MCP server

The wrapper scripts only verify binaries, open settings, and return setup snippets in a stable JSON shape.
