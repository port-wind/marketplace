# Cunzhi Plugin Plan

1. Inspect the upstream Cunzhi repository and confirm its install plus binary model.
2. Scaffold a Cursor-only plugin repository from the existing plugin pattern.
3. Replace the auto-install wrapper with binary detection and install guidance.
4. Add commands, docs, and Pages content for the settings and prompt workflow.
5. Validate locally and add the plugin to `port-wind/marketplace`.
